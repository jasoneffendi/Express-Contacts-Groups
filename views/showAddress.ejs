<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title><%= title%></title>
    </head>

    <body>
        <h1>Halaman Addresses</h1>
        <a href="/">Back to Home</a>
        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Street</th>
                    <th>City</th>
                    <th>Zipcode</th>
                </tr>
            </thead>
            <tbody>
                <% if (data) {data.forEach( row => { %>
                <tr>
                    <td><%=row.id%></td>
                    <td><%=row.street%></td>
                    <td><%=row.city%></td>
                    <td><%=row.zipcode%></td>
                </tr>
                <% })}%>
            </tbody>
        </table>
    </body>
</html>