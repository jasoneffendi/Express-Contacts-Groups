<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <title></title>
    </head>

    <body>
        <thead>
            <tr>
                <th>Name</th>
                <th><%=dataGroup[0].name_of_group%></th>
            </tr>
        </thead>
        <form class="" action="/groups/assign/<%=dataGroup[0].id%>" method="POST">
            <select name="contactId">
                <% if(dataContact) { dataContact.forEach( data => { %>         
                    <option value="<%=data.id%>"><%=data.name%></option>
                <% })} %>
            </select>
            <input type="submit" value="SUBMIT">
        </form>
    </body>
</html>